import { RequiredQueryParamPipe } from './required-query-param.pipe';

describe('RequiredQueryParamPipe', () => {
  it('should be defined', () => {
    expect(new RequiredQueryParamPipe()).toBeDefined();
  });
});
